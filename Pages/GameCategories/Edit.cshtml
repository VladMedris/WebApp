@page
@model VladMedrisWebApp.Pages.GameCategories.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>GameCategory</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="GameCategory.ID" />
            <div class="form-group">
                <label asp-for="GameCategory.GameID" class="control-label"></label>
                <select asp-for="GameCategory.GameID" class="form-control" asp-items="ViewBag.GameID"></select>
                <span asp-validation-for="GameCategory.GameID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="GameCategory.CategoryID" class="control-label"></label>
                <select asp-for="GameCategory.CategoryID" class="form-control" asp-items="ViewBag.CategoryID"></select>
                <span asp-validation-for="GameCategory.CategoryID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
